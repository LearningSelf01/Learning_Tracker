import 'package:flutter/material.dart';

class AdminTrackerPage extends StatelessWidget {
  const AdminTrackerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Admin Tracker (placeholder)')),
    );
  }
}
